<!DOCTYPE html>
<html lang="fr" >
  <head>
    <title>Cloud Dressing: Fais l'inventaire de ton dressing et met en vente ce dont tu ne te sers plus</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="UTF-8">
    <meta name="description" content="">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag    'application' %>
    <script src="https://kit.fontawesome.com/0d31da9d7d.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <%= render 'shared/nav_partial' %>
    <%= yield %>
    <%= render 'shared/footer_partial' %>
    <% # <!-- Retour console (veuillez ne pas effacer - @Fernando) --> %>
      <% if user_signed_in? %>
        <% puts "\n  >> CU is_admin? #{current_user.is_admin}".red %>
        <% puts "  >> CU email     #{current_user.email}".red %>
        <% puts "  >> CU id        #{current_user.id}\n".red %>
      <% else %>
        <% puts "\n  >> CU is not signed_in\n".red %>
      <% end %>
    <% # <!-- end Retour console --> %>
    <%= javascript_include_tag 'application' %>
  </body>

</html>
