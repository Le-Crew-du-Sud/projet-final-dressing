<div class="checkout_area section-padding-80">
  <div class="container">
    <div class="d-flex flex-row flex-wrap justify-content-center">
      <% @attires.each do |item| %>
        <%= render partial: "card", locals: { item: item }%>
      <% end %>
    </div>
  </div>
</div>
